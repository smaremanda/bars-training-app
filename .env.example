# Local development — copy to .env and fill in

# Option A: path to downloaded service account JSON file (local dev)
# Leave GOOGLE_CREDENTIALS empty and put credentials.json in this folder

# Option B: paste the entire contents of credentials.json as one line (Railway)
GOOGLE_CREDENTIALS=
